#include<iostream>
using namespace std;
int main()
{
    float a;
    cout<<"Enter the radius of the circular area: ";
    cin>>a;
    cout<<"The area of the circle is : "<<(3.14*a*a);
    return 0;
}
